<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= video.snippet.title %></title>
  <style>
    :root { --color-bg: #060c06; --color-accent: #54c750; --color-text: #f0f2f0; }
    body { background: var(--color-bg); color: var(--color-text); font-family: sans-serif; margin: 0; }
    .container { display: grid; grid-template-columns: 1fr 380px; gap: 20px; padding: 20px; }
    .player-box { background: #000; border-radius: 12px; overflow: hidden; aspect-ratio: 16/9; }
    .video-info { display: flex; justify-content: space-between; align-items: flex-start; margin-top: 1rem; padding-bottom: 1.5rem; border-bottom: 1px solid #333; }
    .video-meta h2 { margin: 0 0 0.5rem 0; font-size: 1.4rem; line-height: 1.3; }
    .channel-name { color: var(--color-accent); font-weight: 600; font-size: 0.95rem; display: flex; align-items: center; gap: 8px; }
    .channel-name::before { content: ''; display: block; width: 10px; height: 10px; background: var(--color-accent); border-radius: 50%; box-shadow: 0 0 10px var(--color-accent); }
    
    .stats-container { display: flex; gap: 1rem; align-items: center; background: #111; padding: 0.6rem 1.2rem; border-radius: 8px; border: 1px solid #333; height: fit-content; }
    .stat-item { display: flex; align-items: center; gap: 8px; font-size: 0.9rem; font-weight: 600; color: #e0e0e0; }
    .stat-item svg { width: 18px; height: 18px; fill: #888; transition: fill 0.3s; }
    .stat-item:hover svg { fill: var(--color-accent); }
    .divider { width: 1px; height: 20px; background: #333; }
    /* Community Feed */
    .comments { margin-top: 2rem; background: #111; padding: 1.5rem; border-radius: 12px; border: 1px solid #333; }
    .comments-header { display: flex; align-items: center; gap: 10px; margin-bottom: 1.5rem; padding-bottom: 1rem; border-bottom: 1px solid #333; }
    .comments-header h3 { margin: 0; font-size: 1.1rem; color: #fff; }
    .comments-count { background: #222; padding: 2px 8px; border-radius: 12px; font-size: 0.8rem; color: #888; }

    .comment-card { display: flex; gap: 15px; margin-bottom: 1.2rem; }
    .comment-avatar { width: 40px; height: 40px; border-radius: 50%; background: #222; overflow: hidden; flex-shrink: 0; border: 1px solid #333; }
    .comment-avatar img { width: 100%; height: 100%; object-fit: cover; }
    .comment-body { flex: 1; }
    .comment-author { font-size: 0.9rem; font-weight: 700; color: var(--color-accent); margin-bottom: 4px; display: block; }
    .comment-text { font-size: 0.95rem; color: #ccc; line-height: 1.5; margin: 0; }

    /* Right Side Chat Widget */
    .chat-widget { background: #111; border-radius: 12px; border: 1px solid #333; overflow: hidden; height: 600px; display: flex; flex-direction: column; }
    .chat-header { padding: 12px 15px; background: #1a1a1a; border-bottom: 1px solid #333; font-size: 0.85rem; font-weight: 700; color: #888; text-transform: uppercase; letter-spacing: 1px; display: flex; align-items: center; justify-content: space-between; }
    .live-indicator { display: flex; align-items: center; gap: 6px; color: #ff4444; font-size: 0.75rem; }
    .live-dot { width: 6px; height: 6px; background: #ff4444; border-radius: 50%; animation: pulse 1.5s infinite; }
    .chat-frame-container { flex-grow: 1; background: #000; }
    
    @keyframes pulse { 0% { opacity: 1; } 50% { opacity: 0.4; } 100% { opacity: 1; } }
  </style>
</head>
<body>
  <div class="container">
    <div class="main-content">
      <div class="player-box">
        <iframe src="https://www.youtube.com/embed/<%= video.id %>?autoplay=1" width="100%" height="100%" frameborder="0" allowfullscreen></iframe>
      </div>
      <div class="video-info">
        <div class="video-meta">
          <h2><%= video.snippet.title %></h2>
          <div class="channel-name"><%= video.snippet.channelTitle %></div>
        </div>
        <div class="stats-container">
          <div class="stat-item" title="Likes">
            <svg viewBox="0 0 24 24"><path d="M1 21h4V9H1v12zm22-11c0-1.1-.9-2-2-2h-6.31l.95-4.57.03-.32c0-.41-.17-.79-.44-1.06L14.17 1 7.59 7.59C7.22 7.95 7 8.45 7 9v10c0 1.1.9 2 2 2h9c.83 0 1.54-.5 1.84-1.22l3.02-7.05c.09-.23.14-.47.14-.73v-1.91l-.01-.01L23 10z"/></svg>
            <span><%= parseInt(video.statistics.likeCount).toLocaleString() %></span>
          </div>
          <div class="divider"></div>
          <div class="stat-item" title="Views">
            <svg viewBox="0 0 24 24"><path d="M12 4.5C7 4.5 2.73 7.61 1 12c1.73 4.39 6 7.5 11 7.5s9.27-3.11 11-7.5c-1.73-4.39-6-7.5-11-7.5zM12 17c-2.76 0-5-2.24-5-5s2.24-5 5-5 5 2.24 5 5-2.24 5-5 5zm0-8c-1.66 0-3 1.34-3 3s1.34 3 3 3 3-1.34 3-3-1.34-3-3-3z"/></svg>
            <span><%= parseInt(video.statistics.viewCount).toLocaleString() %></span>
          </div>
        </div>
      </div>
      <div class="comments">
        <div class="comments-header">
          <h3>Community Feed</h3>
          <span class="comments-count"><%= comments.length %> Comments</span>
        </div>
        <% comments.forEach(c => { %>
          <div class="comment-card">
            <div class="comment-avatar">
              <img src="<%= c.snippet.topLevelComment.snippet.authorProfileImageUrl %>" alt="User">
            </div>
            <div class="comment-body">
              <span class="comment-author"><%= c.snippet.topLevelComment.snippet.authorDisplayName %></span>
              <p class="comment-text"><%- c.snippet.topLevelComment.snippet.textDisplay %></p>
            </div>
          </div>
        <% }) %>
      </div>
    </div>
    <aside>
      <div class="chat-widget">
        <div class="chat-header">
          <span>Live Chat</span>
          <div class="live-indicator"><div class="live-dot"></div> LIVE</div>
        </div>
        <div class="chat-frame-container">
          <iframe src="https://www.youtube.com/live_chat?v=<%= video.id %>&embed_domain=<%= domain %>" width="100%" height="100%" frameborder="0"></iframe>
        </div>
      </div>
    </aside>
  </div>
</body>
</html>
